# (Simple) multi-player game (dev challenge #5)

# Pre-requisites

- [node/npm](https://github.com/creationix/nvm) (install latest v4)
- gulp-cli
  - `npm install -g gulp-cli`

# Install and run

- `npm install`
- `gulp`
